export enum DashboardRouts {
  REMOTE = '/remote',
  SCHEDULE = '/schedule',
  SETTINGS = '/settings',
  WHITEBOARD = '/whiteboard',
}

export enum BaseRoutes {
  INDEX = '/',
  LOGIN = '/login',
  TIMER = '/timer',
  DASHBOARD = '/dashboard',
}
